import { Card, CardContent, CardHeader } from "@/components/ui/card";

export function NormalizationContent() {
  return (
    <Card>
      <CardHeader>
        <h3 className="text-lg font-bold">Stage 2: Normalization</h3>
      </CardHeader>
      <CardContent>We should show parsing results here.</CardContent>
    </Card>
  );
}
