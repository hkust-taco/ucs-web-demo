import { Card, CardContent, CardHeader } from "@/components/ui/card";

export function DesugaringContent() {
  return (
    <Card>
      <CardHeader>
        <h3 className="text-lg font-bold">Stage 1: Desugaring</h3>
      </CardHeader>
      <CardContent>We should show parsing results here.</CardContent>
    </Card>
  );
}
